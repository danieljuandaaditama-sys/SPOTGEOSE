"use client";
import { useEffect, useState } from "react";
import "./kualitas-data.css";
const fmt=n=>Number(n||0).toLocaleString("id-ID");
export default function KualitasDataPage(){const [data,setData]=useState(null),[error,setError]=useState("");useEffect(()=>{fetch("/api/kualitas-data",{cache:"no-store"}).then(async r=>{const x=await r.json();if(!r.ok||!x.ok)throw new Error(x.error||"Gagal memuat kualitas data");return x}).then(setData).catch(e=>setError(e.message));},[]);if(error)return <main className="quality-page"><div className="quality-error">{error}</div></main>;if(!data)return <main className="quality-page"><div className="quality-loading">Memuat Kualitas Data…</div></main>;return <main className="quality-page"><section className="quality-hero"><div><div className="eyebrow">QUALITY CONTROL CENTER</div><h1>Kualitas Data</h1><p>Pemeriksaan dasar pada latest records. Temuan tidak otomatis berarti error; konteks operasional tetap perlu diperhatikan.</p></div></section><section className="quality-cards"><article className="quality-card primary"><span>Total Temuan</span><strong>{fmt(data.totals.total_temuan)}</strong><small>temuan rule dasar</small></article><article className="quality-card"><span>Assignment Terdampak</span><strong>{fmt(data.totals.assignment_terdampak)}</strong><small>akumulasi temuan</small></article><article className="quality-card"><span>Jenis Anomali</span><strong>{fmt(data.totals.jenis_anomali)}</strong><small>rule aktif</small></article><article className="quality-card warm"><span>Mapping Matched</span><strong>{fmt(data.meta.mapping_matched)}</strong><small>latest records</small></article></section><section className="panel anomaly-panel"><div className="panel-heading"><div><div className="eyebrow">PEMERIKSAAN DASAR</div><h2>Temuan Data</h2></div></div><div className="anomaly-table-wrap"><table className="anomaly-table"><thead><tr><th>ID</th><th>Anomali</th><th>Temuan</th><th>Level</th><th>Rule</th></tr></thead><tbody>{data.anomalies.map(r=><tr key={r.anomaly_id}><td><span className="id-pill">{r.anomaly_id}</span></td><td><strong>{r.nama_anomali}</strong></td><td>{fmt(r.jumlah_temuan)}</td><td><span className="severity medium">{r.level}</span></td><td>{r.deskripsi_rule}</td></tr>)}</tbody></table></div></section></main>}
